<Window x:Class="TerraCreator_Re_Insider.forms.ImportProject"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TerraCreator_Re_Insider.forms"
        mc:Ignorable="d"
        Title="导入项目" Height="262" Width="490" Icon="/forms/Icon.png" Background="{DynamicResource TerraCreatorBackgroundNormalColor}">
    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD"/>
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070"/>
        <SolidColorBrush x:Key="Button.MouseOver.Background" Color="#FFBEE6FD"/>
        <SolidColorBrush x:Key="Button.MouseOver.Border" Color="#FF3C7FB1"/>
        <SolidColorBrush x:Key="Button.Pressed.Background" Color="#FFC4E5F6"/>
        <SolidColorBrush x:Key="Button.Pressed.Border" Color="#FF2C628B"/>
        <SolidColorBrush x:Key="Button.Disabled.Background" Color="#FFF4F4F4"/>
        <SolidColorBrush x:Key="Button.Disabled.Border" Color="#FFADB2B5"/>
        <SolidColorBrush x:Key="Button.Disabled.Foreground" Color="#FF838383"/>
        <Style x:Key="TerraCreatorButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Pressed.Border}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource Button.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Button.Disabled.Border}"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Height="246" Width="490">
        <Label x:Name="ImportLabel" Content="导入项目" FontSize="22" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,0,0"/>
        <Label x:Name="DescribeLabel" Content="在tModLoader中创建项目后&#xA;选择你创建的项目的文件夹位置&#xA;(部分软件会阻止TrC访问User文件夹)" Height="56" Width="224" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,53,0,0"/>
        <Label x:Name="ChooseFolderLabel" Content="选择文件夹:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,125,0,0"/>
        <TextBox Style="{DynamicResource TerraCreatorTextBoxStyle}" x:Name="ChooseFolderTextBox" TextWrapping="Wrap" Width="325" HorizontalAlignment="Left" VerticalAlignment="Top" Height="27" Margin="88,123,0,0"/>
        <Button x:Name="ChooseFolderButton" Content="选择" Width="47" Height="27" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="418,123,0,0" Click="ChooseFolderButton_Click"/>
        <Label x:Name="ModNamespaceLabel" Content="输入Mod内部名称:" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,155,0,0"/>
        <CheckBox x:Name="CreateTrCprojFileCheckBox" Content="创建TrC项目文件(.trcproj)" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,207,0,0"/>
        <TextBox x:Name="ModNamespaceTextBox" TextWrapping="Wrap" Width="338" Height="27" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="127,153,0,0"/>
        <Button Style="{DynamicResource TerraCreatorButtonStyle}" x:Name="CreateButton" Width="97" Height="27" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,25,18" Content="开始" Click="CreateButton_Click"/>
    </Grid>
</Window>
